@{
    ViewData["Title"] = "Dashboard de Alumnos";
}

<div class="container mt-4">
    <h2 class="mb-4">Dashboard de Alumnos</h2>
    
    <div class="row">
        <div class="col-md-6">
            <div class="card mb-4">
                <div class="card-body">
                    <h3 class="card-title">Distribución por Edad</h3>
                    <canvas id="ageDistributionChart"></canvas>
                </div>
            </div>
        </div>
        <div class="col-md-6">
            <div class="card mb-4">
                <div class="card-body">
                    <h3 class="card-title">Promedios por Edad</h3>
                    <canvas id="averagesChart"></canvas>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script src="~/js/alumnosCharts.js"></script>
}